# dependencies (none today — vanilla JS, no build — but never commit them if added)
node_modules/

# build artifacts / packaging
*.zip
*.crx
*.pem

# os / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# secrets (none needed today, but never commit them)
.env
.env.local
